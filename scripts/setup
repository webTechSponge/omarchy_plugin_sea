#!/usr/bin/env bash
set -euo pipefail
for tool in bash curl jq git flock timeout omarchy omarchy-shell quickshell qmllint perl magick; do
  command -v "$tool" >/dev/null || { echo "Missing required system tool: $tool" >&2; exit 1; }
done
printf 'Native dependencies available. '
omarchy-shell shell ping
